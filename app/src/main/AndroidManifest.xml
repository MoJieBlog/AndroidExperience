<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lzp.experience">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".application.MApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/BaseAppTheme">
        <activity android:name=".main.MainActivity"/>

        <activity android:name=".MPermissionTestActivity"
            android:screenOrientation="portrait"/>

        <activity android:name=".main.MSplashActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".image.ColorMatrixStudyActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".ConstraintLayoutTestActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".viewpager.ViewPagerListActivity"
            android:screenOrientation="portrait"/>
    </application>


</manifest>